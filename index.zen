(page :lang "en"
    (head
        (meta :charset "utf-8")
        (meta :name "viewport" :content "width=device-width, initial-scale=1")
        (title Flux Cart)
        (stylesheet :at "css/app.css"))

    (body
        (section :id "flux-cart")
        (script :at "js/bundle.js")))
